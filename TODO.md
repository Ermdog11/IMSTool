# IMS Tool — Running To-Do List

## Pending
- [ ] **Convert index.html tabs to real URL paths (Jeff, 2026-09-10, approved)** — client-side router w/ History API + `vercel.json` rewrite so each section is a real path (`/alerts`, `/digest`, `/social`, `/chat`, `/backlog`, `/recruiting`, `/trending`, `/podcasts`, `/youtube`, `/bluesky`, `/settings`; `/editor` already separate). Fixes the broken Back button (the in-section "← Back to Alerts" buttons push history instead of going back). Stays one app/one JS bundle — shared scan state persists across sections. Jeff wants to rename some paths — AWAITING his list.
- [ ] **Knowledge-base ownership & portability (Jeff, 2026-09-10)** — denoted choice per writer: publication-owned / writer-owned / shared. Writer-owned = self-serve "export my knowledge base" (their `content_items` slice + style profile, portable format) they can take to another outlet or keep. Ownership field on `memberships`, set at onboarding; departure handling honours it. Recruiting pitch + retention lever. On top of the KB foundation. Full notes: memory `project-newsroom-knowledge-base`.
- [ ] **Per-writer content catalog (Jeff, 2026-09-10) — infrastructure, several features depend on it.** "Catalog every piece of content by every writer so it can add information and mirror the writing style." One index per writer of everything they've published. Powers: (a) richer style profiles — distilled from the full body of work, not just ~15 pasted samples (`api/style-profile.js`); (b) related-links / internal linking from the writer's + the site's full archive, not just the front page (`relatedArticleIndex()` in `api/copyedit.js`); (c) context-adding in Copydesk drawing on what the site has already reported. Sourcing reality: SaaS customers on WordPress/Ghost = full auto-catalog via CMS API (ties to Publish connector work). 247Sports/InsideMDSports = author page gives the article list + non-paywalled text; paywalled bodies still need paste or a Jeff-provided export (known dead end per [[project-enterprise-productization]]). Storage: Blob now → Supabase later. Build after login (needs the per-writer/per-site model).
- [ ] **Copydesk: writer pick-one-of-3 related-links UI (Jeff, 2026-09-05 spec)** — explicit writer-driven "pick one of 3 suggested related links, insert near mid-article" control, distinct from the silent auto-insertion. Build alongside the per-writer content catalog above (shares the archive index).
- [ ] **YouTube capture is thin — deprioritized by Jeff 2026-09-10, but diagnosis done:** likely causes (1) **quota exhaustion** — `api/youtube.js` search.list = 100 units each; scan runs every 30 min, 15-min cache always misses, batch=4 → ~48×4×100 ≈ 19,200 units/day vs 10,000 free → API 403s from ~midday on; (2) `searchRotation` is module-level, resets on Vercel cold starts → often stuck searching the same first 4 of 20 term-groups. Fixes when revisited: time-based rotation (`Math.floor(Date.now()/1800000)*batch`), raise `YT_CACHE_MS` to ~60 min + bigger batch on refresh, move YouTube to its own 2-hr cron like roster-check, surface the quota error in the UI, and have Jeff request a YouTube API quota increase (Google grants these). Also loosen: drop "rumors/insider report/fan nation" from `aiChannelPatterns`, `[!?]{3,}`→`{4,}`, sub floor 400→250.
- [ ] **Audience analytics section (Jeff, 2026-09-10) — flagship, mid-term.** Publisher connects GA4 / Chartbeat / Parse.ly / Search Console / Facebook+Instagram Page Insights (per-tenant, encrypted in Supabase). Delivers: real-time alerts (piece spiking/cratering, uncovered trending topic), daily/weekly insights digest (top performers, best publish window by day-of-week, format/topic/headline patterns, evergreen decay), proactive suggestions, and conversational Q&A ("what's been working best for us?") that JOINS analytics with [[project-newsroom-knowledge-base]] to explain *why* (by writer / category / publish time). Integration reality: GA4 + Chartbeat + Parse.ly + Search Console all have real APIs; FB/IG = own-page insights only (CrowdTangle retired 2024, no broad social listening). Build order: GA4+Chartbeat, then FB Page Insights, then the Q&A layer. Needs login + knowledge base + the scheduled-agent runner first. Full spec: memory `project-audience-analytics`. **Includes writer analytics (Jeff, 2026-09-10):** per-writer views + engaged time-on-page + virality signal, sliced by content type (from the KB), plus loose proactive nudges ("your last two [type] pieces averaged X% more views than baseline"). Publisher view + per-writer view. Best demo of the analytics × KB join.
- [ ] **Content Editor: auto-suggest a relevant video from the publisher's own library (Jeff, 2026-09-10)** — video analog of the related-links feature. Publisher configures a video source in settings (YouTube channel URL / Vimeo / video RSS); when a writer edits a draft, the tool matches a relevant video and embeds/suggests it in the copy. Needs per-tenant config → build after login. Reuse the related-links matching approach from `api/copyedit.js`.
- [ ] **Login system for InsideMDSports team (Jeff, 2026-09-10, approved — scope: team login, NOT full multi-tenant signup yet)** — Supabase (Postgres + Auth: magic-link + Google). Model: `users`, `sites` (InsideMDSports = one site now, generalizes later), `memberships(user_id, site_id, role)` role ∈ publisher/editor/writer, `alert_prefs`. `/login` page, session gating on index.html + editor.html + all API endpoints, publisher-only Team page (invite by email, set role, toggle who receives which alerts). Wire `_notify.js` + breaking-news email to `alert_prefs` instead of hardcoded recipient. Jeff needs to create the free Supabase account + provide keys. Sequenced AFTER the page conversion. ~2-3 days. This is the [[project-enterprise-productization]] "biggest missing piece" (persistence + auth).
- [ ] **Alert on article SAVE, not just Send-to-publisher (Jeff, 2026-09-10)** — today `api/submit-article.js` emails the publisher only on `action:'submit'`; `action:'save'` (draft) is silent. Jeff wants a save to also notify the publisher "and others if they get permissions." DECIDED (Jeff, 2026-09-10): notify on FIRST save of a new article only. Recipients come from the login system's `alert_prefs` (publisher decides who) — so build this as part of / right after login, not standalone.
- [ ] **Roster watcher — add a page/tab (Jeff, 2026-09-10)** — `api/roster-check.js` IS active (Vercel cron `30 11 * * 1,3,5` = Mon/Wed/Fri 7:30am ET; checks 5 UMD rosters off umterps.com Sidearm pages, emails + pushes on diff). Currently NO UI — email/push only, snapshots in private Blob. Build: (1) `api/roster-check.js` also appends diffs to a `roster-snapshots/roster-changes.json` blob (capped history); (2) new GET `api/roster-status.js` reads the 5 snapshots + the change log, no side effects; (3) new `/roster` section in index.html — nav tab, `page-N`, `loadRoster()`, collapsible current roster per team + dated "Recent changes" list; (4) add `/roster` to `TAB_PATHS`, vercel.json rewrite + headers. Self-contained, independent of login — BUT Jeff (2026-09-10) wants login built first, then this.
- [ ] **Inserts: true drag-and-drop placement (Jeff, 2026-09-10) — optional polish.** Now they drop at a chosen default position (top/early/middle/end) with a per-article override menu + live preview (`renderInsertPreview` in editor.html). Jeff floated drag-and-drop as an alternative; only build it if the position menu proves too coarse — real drag-drop into the rendered article means contenteditable block handling + mapping back to the markdown/HTML sent to `submit-article.js`.
- [ ] **House style guide = the publisher's own writing (Jeff, 2026-09-10)** — Jeff wants the default house style (the baseline every Copydesk edit uses when no specific writer is picked) to capture HIS voice, since he's the founder/lead writer. Build: a "Build house style from samples" action on the House style tab — paste 3-8 articles → Claude generates a house style guide (reuse `api/style-profile.js` approach with a houseStyle target, or a small `api/house-style.js`). Store to the same `ims_editor_styleguide` localStorage / eventually per-site config. Interim: hand-write the DEFAULT_GUIDE to match his voice.
- [ ] **One "social scout" feed (Jeff, 2026-09-10)** — merge the "Hot social" tab (`scanSocial`, page-2) and the "Bluesky" tab (`loadBluesky`, page-9) into a single social-discovery feed for finding news. Distinct from the internal `/analytics` (own-account performance). X stays out (not viable free — see [[project-imstool-overview]] dead ends). Realistic sources: Bluesky + Reddit + whatever else is open. One tab, one loader, drop the redundant one; keep the section-routing indices sane.
- [ ] **Scouting report generator (Jeff, 2026-09-11)** — feed a team name + week number, auto-pull: depth chart (Ourlads), roster (the school's own site), recent results, key stats, coaching background, matchup advantages vs. Maryland. Automates the manual UConn research Jeff just did by hand via Ask-the-editor. Real multi-source build: Ourlads has no API (scrape their depth-chart pages, format is per-team, needs mapping); school sites vary (reuse roster-check.js's Sidearm-template parser as a starting point, most D1 athletics sites use it); recent results/stats need a source (ESPN/CBS scoreboard scrape, or the team's own schedule page); coaching background + matchup analysis is a Claude synthesis pass over all of the above. Shape: `api/scouting-report.js` (team, week) -> gathers each source (best-effort per source, degrade gracefully) -> one Claude call synthesizes -> returned as a doc the writer can drop into an article. Button lives in the Content Editor. Bigger build (multi-source scraping + synthesis) — sequence like analytics/invoicing, own focused session.
- [ ] **Social media post generator as a standalone tool (Jeff, 2026-09-11)** — promote the existing per-alert `getSocial()` card action (X/Twitter, Facebook, Instagram caption) into a standalone Content Editor tool: run it on any finished article, add a thread version for long-form pieces, and where possible let the writer send straight to Buffer or another scheduler. Buffer has a real API but needs OAuth app registration (co-work, same shape as the analytics connections) — that part is a stretch goal; the generate-4-platform-versions part needs no new integration and is a quick build on top of what already exists.
- [ ] **Newsletter builder (Jeff, 2026-09-11, scope decided) — full send, not just a draft.** Auto-generate a daily/weekly READER-FACING newsletter (distinct from the existing internal digest emails staff already get) from published articles + scan results + trending topics: top 5 stories, one-line summaries, a lede write-up. DECIDED: a draft-only handoff is "a drop-off" in value — build IMSTool to actually manage the subscriber list and send the newsletter itself. This doubles as a sellable feature for publishers who don't already run a newsletter platform; keep a draft-handoff mode too for outlets that have their own tool and just want the content. Needs: Supabase `subscribers` table (site_id, email, subscribed_at, status) + unsubscribe handling, a template/layout, pulls from the knowledge base once it exists (or scan results in the meantime), and a send mechanism (reuse `_mailer.js` patterns but this is bulk/subscriber mail, not the internal alert path — likely wants a real transactional mail provider at subscriber-list scale, not just Gmail SMTP). Real module, sequence with the other DB-dependent features.
- [ ] **Stat lookup engine (Jeff, 2026-09-11)** — type a player name, get career stats / game logs / comparisons in one query, instead of manually digging through ESPN or the school's stats site (named examples: Scruggs' tackles, Delhomme's blocked kicks, Jeshaun Jones' career numbers). Shares scraping infrastructure with the Scouting Report Generator above (ESPN player pages + school stats pages) — build together, same data sources. `api/player-stats.js`: name -> resolve to the right player page (name-collision handling reused from scan.js's approach) -> pull the stats table -> Claude formats/answers comparisons. Button in the Content Editor, and usable from Ask-the-editor once built.
- [ ] **Historical records database (Jeff, 2026-09-11)** — Maryland (or any team, must be easily reconfigurable) all-time leaders, season records, game records, searchable by stat — prevents errors like citing a record from memory (named examples: Juan Dixon's scoring record, Taulia's passing record, Tai Felton's receiving record). Unlike the live Stat Lookup above, this is a curated reference table, not scraped fresh each time — someone (Jeff, a writer, or an initial Claude research pass) populates it once per team, editable afterward. Shape: a `records` table (Supabase) keyed by site/team — category, holder, value, date/season, source — plus a simple search UI and an admin edit screen. "Easily customizable for any team" = the whole point of it being data in a table, not hardcoded, matching the multi-tenant direction.
- [ ] **RESEARCH SUITE (Jeff, 2026-09-11) — umbrella for the four items above.** Scouting Report Generator, Stat Lookup Engine, Postgame Quote Transcripts, and Historical Records Database are one coherent writer-research toolkit: "everything I'd otherwise Google or dig up by hand, in one place in the Content Editor." Share scraping infra where possible (school sites, ESPN) and surface as one "Research" panel/button group rather than four scattered buttons. Build as a set once started, in roughly this order: Historical Records (pure data entry, no scraping risk) -> Postgame Quotes (feed already identified, just needs the real RSS/search URL) -> Stat Lookup -> Scouting Report (hardest, most sources).
- [ ] **Article templates in Copydesk (Jeff, 2026-09-11)** — pick a template (game recap, transfer-portal piece, positional ranking, interview-driven feature), paste raw notes/info, get an ~80%-done draft in that structure. Needs to run fast (Jeff's bar: ~20-30 sec). Shape: a template dropdown next to Mode/Headlines in Copydesk; each template = a structural prompt (section order, what belongs where) layered on top of the existing house-style/voice system, not a separate tool. Reuse the existing `submit_copyedit` tool-call plumbing for speed.
- [ ] **Fact-checker against own archive (Jeff, 2026-09-11)** — before publishing, cross-reference claims in the draft against the newsroom's own past articles to flag contradictions (example: a "three-year drought" claim that contradicts something published last season). Hard-depends on [[project-newsroom-knowledge-base]] existing and being searchable — log as a knowledge-base-powered feature, not standalone. Once the KB exists: a claims-extraction pass over the draft, a search against `content_items`, a Claude comparison step, surfaced as an extra "Questions to resolve"-style flag.
- [ ] **Multi-writer assignment board (Jeff, 2026-09-11)** — upgrade from the current ad hoc claim-in-Team-Chat (`claimStory()` in index.html, just posts a chat line). Real version: writers see scan results with claim/assignment state, claim a story (visible to everyone, prevents duplicate coverage), see who's on what. Needs the login/roles system (done) to attribute claims to real people. Shape: a `claims` table (story identity - probably normalized headline/URL hash - + claimed_by + claimed_at + status) + a board view, likely replacing or sitting alongside the current Team Chat tab.
- [ ] **Postgame quote transcripts (Jeff, 2026-09-11)** — button in the Content Editor to pull quote transcripts from a team's games (ASAP Sports or similar postgame-quote service). Quick look at asapsports.com: homepage has no exposed search API or team-filter — content lives behind "FastScripts Archive" / sport category pages (`showcat.php?id=N`) that need real browsing (not a one-shot fetch) to map out team/event IDs before this is scrapable reliably. Next step: browse the actual archive/search flow (Claude Browser, not WebFetch) to find Maryland football/basketball event listings and a transcript URL pattern, then build `api/postgame-quotes.js` the same shape as `podcast-episodes.js` (search/list -> pick -> insert as reference, NOT auto-inserted into the article). Worth it if buildable — real value for writers.
- [ ] **Reddit — re-asked by Jeff (2026-09-11), still blocked per prior investigation.** Vercel server IPs get 403'd for anonymous Reddit JSON, and Reddit locked self-serve API app creation for new developers (see "Reddit fix — dead end" in Done section, 2026-09-05). No workaround unless Jeff has an existing/grandfathered Reddit API app+key. Asked him; awaiting answer. Don't re-attempt the plain-fetch approach again, it's confirmed blocked.
- [ ] **Invoicing tool (Jeff, 2026-09-10)** — writers/contractors submit monthly invoices to the publisher through the app; publisher marks Paid (paid date, method, reference); **paid records persisted immutably for taxes** with a year-end per-payee CSV export for 1099-NEC prep. Needs login (done) + Supabase: `invoices` + append-only `payments` + optional `payee_tax_info` tables; `/invoices` section (role-split view); `api/invoices.js`; submit-notification email. NOT a payment processor. ~half a day. Full spec: memory `project-invoicing`.
- [ ] **Photo upload tool in Content Editor (Jeff, 2026-09-10)** — writers attach images to a draft. Storage: Vercel Blob (already a dependency) or the eventual Supabase storage bucket. Include alt-text field. Decide: images embedded in the draft HTML vs. a separate attachments list on the draft doc.
- [ ] **Speed up scans — currently ~3 min (Jeff, 2026-09-10)** — bottleneck is the single Claude call in `api/scan.js` rating ~110 stories with a ~5k-token prompt and up to 20k output tokens. Fixes that don't lose content: (1) prompt-cache the static watchlist+rules block (identical every scan) via `cache_control` — big TTFT win; (2) split the rating into 2-3 parallel chunked calls, merge results — cuts wall-clock ~2-3x; (3) trim first-pass output (drop `summary`, generate only for displayed items); (4) try Haiku 4.5 for the first-pass rating, keep Sonnet for deep-read. Do 1+2 first, measure. NOTE also: `api/scan.js` `console.log` reports timing-relevant counts.
- [ ] **Twitter/X scan** — parked. API costs $200/mo minimum for meaningful read access; free workarounds (Nitter) are dead. Bluesky scan added as free alternative; big tweets get picked up via news sites anyway.
- [ ] **Instagram scan** — parked. Requires Instagram Business/Creator account + Meta developer app; limited to 30 hashtag searches/week (#terps, #marylandterrapins). Revisit if Jeff sets up a Business account.
- [x] **Reddit fix — dead end, dropped (2026-09-05).** Reddit blocks Vercel's servers (403) for anonymous JSON access, and as of 2026 Reddit has also locked self-serve API app creation for new developers — old.reddit.com/prefs/apps "create app" no longer works (shows a broken "read our full policies here" message with no way to accept). Getting API access now requires a manual developer-request form to Reddit with a high solo-developer rejection rate. Jeff has no pre-existing (grandfathered) Reddit app/key, so there's no workaround. Not revisiting unless Reddit changes this policy or Jeff acquires a grandfathered app later.
- [ ] **YouTube integration** — search YouTube for Terps content. Needs `YOUTUBE_API_KEY` in Vercel (free Google Cloud key).
- [ ] **Fix morning-brief.js and digest.js** — still use the old broken web_search approach; need same RSS rewrite as scan.js.
- [ ] **Delete debug endpoints** — api/debug-rss.js, api/debug-podcasts.js, api/debug-reddit.js once everything is stable.
- [ ] **ListenNotes (optional)** — free quota (300/mo) exhausted this month. Could re-add next month as supplement; currently replaced by free iTunes/RSS approach.

- [ ] **Deeper podcast transcript search (Jeff, 2026-09-10)** — current `api/transcripts.js` scans YouTube auto-captions of a fixed set of national shows for buried Maryland mentions, triggered manually from the Podcasts tab. Jeff wants it deeper / more automatic: more shows, run it on every scan (not on-demand), and go past YouTube captions where possible (Podscribe / Listen Notes transcripts, Apple transcripts, Whisper on the audio for shows without captions). Watch cost/time — Whisper transcription and more shows add real latency; likely a separate cron like roster-check rather than inline with the news scan.
- [ ] **Podcast transcript search** — scan YouTube auto-captions for shows that post there (catches Maryland mentions buried inside episodes)
- [ ] **Wikipedia edit watch** — watch ~30 Terps pages for edits (free API, catches coaching changes/commitments fast)
- [ ] **Message board activity spike detector** — only free boards are Testudo Times comments (JS-heavy, not simply scrapeable) and Reddit (blocked until API credentials). Revisit after Reddit API is set up.
- [ ] **Local TV RSS** — WBAL, WJZ, WUSA9 sports feeds
- [ ] **MaxPreps/HS sports** — recruit performances before national radar
- [ ] **Shared/server-side source blocklist** — source blocking is per-browser only right now (localStorage `blockedSources`). If the team wants a shared blocklist, move it to a stored config the scan/podcasts endpoints read.
- [ ] **Related-articles picker on Editorial Desk (Jeff, 2026-09-05, spec confirmed)** — scan the submitted/edited article, surface 3 candidate related articles from the host site's recent coverage (reuse `relatedArticleIndex()` in `api/copyedit.js`, already scrapes the 247 Maryland landing page), let the writer click one to insert a link near the middle of the article. Distinct from Copydesk's existing automatic link insertion (which the AI does silently during rewrite) — this is an explicit, writer-driven pick-one-of-3 UI. Supersedes the earlier vaguer "SEO tool" idea; build this spec.
- [ ] **SHELVED — desktop push notifications, revisit later (Jeff, 2026-09-05).** The Web Push infra (VAPID keys, service worker, subscription storage in Vercel Blob) is fully built and confirmed working server-side — `sendPush()` reports a successful delivery to Chrome's push service (FCM). The notification still doesn't visually appear on Jeff's Windows machine even after fixing the Chrome-side site permission (it was "denied", now "granted", fresh subscription created). Next debugging step, when revisited: check Windows' own notification gate, not Chrome's — (1) Windows Action Center (taskbar, near the clock) for a queued-but-unseen toast, (2) Windows Settings → System → Notifications → confirm "Google Chrome" is allowed, (3) Focus Assist / Do Not Disturb state. Stopped here per Jeff: "cant keep wasting time" — don't restart this without him asking. Everything else built this session (roster-change email alerts, breaking-news email) works independently of push and is unaffected.
- [ ] **"Ombudsman" coverage-ideas agent (Jeff, 2026-09-05)** — reads InsideMDSports' own published articles + web coverage, tracks game results and the upcoming schedule, and proactively suggests coverage/story ideas and gaps. Overlaps with the existing "Story backlog" tab (`generateBacklogFromAlerts` in index.html — generates 6 angles from the current scan alerts); this would be broader: schedule/results-aware, and reads competitor coverage explicitly rather than just working off scan alerts. Design as a "scheduled agent" per [[project-enterprise-productization]]'s three-layer model rather than a one-off script. Not started yet — queued behind the Editorial Desk drafts/VIP-tier feature in progress.

## Done
- [x] Bing News RSS (3 queries — independent index from Google)
- [x] Direct feeds: Testudo Times, The Diamondback, PressBox, UMTerps.com official
- [x] Regional site queries: Baltimore Sun, Washington Post, Baltimore Banner
- [x] Rival recruiting battle queries ("beats out Maryland", visit/decision news)
- [x] The Athletic (nytimes.com) targeted queries
- [x] 20 OR-grouped Google News feeds covering all 250 search terms
- [x] 36-hour story window
- [x] Trending topic cap (3 per name, rated by newsworthiness) + 🔥 Trending overflow tab with hotlinks
- [x] 🎙️ Podcasts tab — free iTunes/RSS approach: ~20 curated shows + episode discovery across all podcasts
- [x] Cannabis podcast filtering, Dolphins Malik Washington filtering
- [x] Inside The Black And Gold RSS feed added to scanner
- [x] Prioritize breaking/commits/committed stories (auto 4-5 rating)
- [x] Reddit noise filters (media posts, game threads, discussion rated low)
- [x] Diamond Stone removed from search terms (vinyl decal spam)
- [x] Republished article detection + ♻️ Republished tab
- [x] Jeff's own content (InsideMDSports/247Sports Maryland) excluded everywhere
- [x] HTML caching disabled so UI updates reach browsers immediately
- [x] Blocked low-quality aggregator podcast shows by name ("Maryland Terrapins Football News Today", "Maryland Basketball Football News Today") — filtered out of both the curated feed parse and the iTunes discovery results in api/podcasts.js
- [x] "Block this source" button added to article cards (public/index.html) — hides all current + future stories from that source, stored per-browser in localStorage (`blockedSources`), same pattern as the existing Flag button. Blocked sources are also sent to api/scan.js (folded into its `excluded` list) and api/podcasts.js (`?blocked=` query param) so filtering happens server-side too.
- [x] "Block show" button on podcast cards + Settings → "Sites you blocked" list with per-entry unblock (public/index.html) — gives a way to view and undo blocks.

## Context for future sessions
- **Alumni story cap (no duplicate alumni coverage):** api/scan.js has a post-rating cap step — any story Claude categorizes as "alumni" gets a hard limit of 1 main-feed slot regardless of which alumnus it's about (general Terps topics get 3). This exists so the main feed doesn't fill up with 4-5 stories about the same former player's NFL/NBA game. Overflow stories aren't dropped — they go to the 🔥 Trending overflow tab. This is intentional, established behavior — don't "fix" it as a bug if it looks like stories are being hidden.
- **Editorial voice/style rules** (for anything that touches article generation, not the news scanner itself): AP style, no bold/headers unless asked, no filler phrases or sports clichés, cite 247Sports ratings only (never Rivals/ESPN), research before writing.
- **Jeff is non-technical** — walk through git/terminal steps explicitly when they come up, don't assume familiarity with command line basics.
- **Source blocking is per-browser** (localStorage `blockedSources`) — there's no server-side/shared blocklist. There IS now a UI to view and unblock (Settings → "Sites you blocked"). A shared blocklist would need the endpoints to read from stored config instead of the request body — see Pending.
